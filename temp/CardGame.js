let NiuNiu=0;
let YangYang=0;
let n = prompt("请输入纸牌数");
let m= prompt("请输入纸牌上的数字");

if(isNaN(n)||isNaN(m)){
  for(let count=0;count<n;count++){
    if(count/2){
      NiuNiu += Max(m);
      delect(Max(m));
    }
    else Yangyang +=Max(m)
    delect(Max(m));
  }
}

